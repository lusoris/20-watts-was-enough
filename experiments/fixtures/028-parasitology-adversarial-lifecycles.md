# Fixture F-028 — Parasitology-derived adversarial lifecycle boundaries

<!-- markdownlint-disable MD013 -->

- **Status:** protocol-complete specification-only CPU research contract; no generator, runner, seed pack, manifest, raw output, statistical result, or measured energy exists
- **Claims addressed:** [C-1569](../../research/claims.md#c-1569), [C-1570](../../research/claims.md#c-1570), [C-1571](../../research/claims.md#c-1571), and [C-1572](../../research/claims.md#c-1572)
- **Source audit:** [parasitology: adversarial surfaces, lifecycle commitment, host capture, and competitive release](../../research/audits/2026-08-27-parasitology-adversarial-lifecycles.md)
- **Fixture ID:** `F-028`
- **Protocol IDs:** `PAR-X01`--`PAR-X04`
- **Execution state:** absent. There is no `experiments/workstation/fixture-028/` directory and no `experiments/workstation/manifests/fixture-028.json`; creating either later would not itself authorize a scientific comparison
- **Authority:** every hypothesis, threshold class, comparison, table, and predicted direction in this file is **NO_RESULT** until an independently frozen implementation, development/confirmation split, valid execution receipt, complete raw artifacts, registered analysis, and calibrated resource boundary exist

F-028 converts four engineering consequences of the source audit into synthetic falsification contracts. It does not reproduce a parasite experiment, infer an unobserved biological mechanism, recommend adversarial behavior, or claim an energy advantage. The fixture asks whether the proposed AI translations beat complete conventional robustness, hierarchical-control, security, and ecological-control nulls after lineage, lifecycle, authority, population, and cost boundaries remain explicit.

| Protocol | Claim | Bounded engineering question | Current result |
| --- | --- | --- | --- |
| `PAR-X01` | C-1569 | Can latent identity or function be recovered under unseen surface rotations and compositions without false certainty in non-identifiable worlds? | **NO_RESULT** |
| `PAR-X02` | C-1570 | Does a stateful, future-constraining commitment policy improve the service--recovery--cost frontier beyond ordinary options and reserves? | **NO_RESULT** |
| `PAR-X03` | C-1571 | Which assurance stack detects observation, evaluator, router, authority, or telemetry capture while focal model weights remain fixed? | **NO_RESULT** |
| `PAR-X04` | C-1572 | Can target suppression increase total harm by releasing or substituting another threat family, and can a population-aware controller prevent it? | **NO_RESULT** |

The four central claim links above are the fixture side of the required reciprocal traceability. Claim-side records must link this exact file before the generated test-coverage report may count reciprocity.

## Scope and scientific boundary

The biological sources establish organism- and experiment-specific observations: antigenic surface diversity in *Trypanosoma brucei*; AP2-G as an essential causal regulator of commitment in tested *Plasmodium* systems; a baculovirus gene intervention that changes host climbing; and competitive release in mixed rodent-malaria infections. Those findings constrain the artificial stressors. They do not establish an unchanged latent function beneath biological surfaces, AP2-G sufficiency across contexts, an unchanged-model control-plane attack, or that the proposed artificial policies are useful, efficient, safe, or biologically equivalent.

F-028 therefore keeps two authorities separate:

1. **source authority:** belongs to the cited primary experiments and the bounded statements in the source audit; and
2. **fixture authority:** could later support only a result about the frozen artificial data-generating process and compared systems.

The fixture cannot validate a biological claim by simulation. A successful source reproduction cannot validate the AI translation. A successful AI comparison cannot generalize to deployed systems without another transport and safety evaluation.

## Common notation and units

| Symbol | Meaning | Unit |
| --- | --- | --- |
| $k$ | independent artificial system-instance index | dimensionless integer |
| $t$ | step index or elapsed time, named by each track | step or second (s) |
| $z$ | latent entity, regime, authority, or threat state | typed categorical state |
| $e$ | visible surface or rendering epoch | dimensionless categorical index |
| $o$ | observation produced from latent state | track-native observation vector |
| $a$ | system or adversarial action | typed categorical/action vector |
| $\ell$ | preregistered task loss | dimensionless after frozen scaling |
| $H$ | registered evaluation horizon | step or second (s) |
| $B$ | retained, transmitted, or written data | byte (B) |
| $T_{\mathrm{wall}}$ | process wall time | second (s) |
| $T_{\mathrm{CPU}}$ | total CPU time | CPU-second (CPU-s) |
| $M_{\mathrm{peak}}$ | peak resident memory | byte (B) |
| $N_{\mathrm{op}}$ | logical operation count under the frozen counter | dimensionless count |
| $E$ | calibrated electrical energy, unavailable until later physical measurement | joule (J) |

Synthetic operation counts, model invocations, bytes, and seconds remain separate. None may be converted to joules. A later energy result must state meter, sampling interval, machine boundary, idle treatment, uncertainty, hardware, software, and repeated-run protocol.

## Question and hypotheses

The common question is whether exposing the source audit's hidden state boundaries yields a measurable robustness or resource advantage after the strongest mature null already receives the same observations, action authority, capacity, tuning, and total budget.

The registered hypotheses are directional but deliberately conditional:

1. **H1 — surface is not identity.** Under unseen surface compositions, a surface-only detector has higher worst-epoch false-merge or false-split risk than an active-challenge or provenance-qualified system when latent behavior remains distinguishable.
2. **H2 — non-identifiability is prior-bounded.** When `same` and `different` link labels induce the same permitted joint observable distribution, no arm can beat the declared prior-only Bayes risk. In the equal-prior control, accepted link decisions cannot beat chance; a calibrated selective arm can reduce risk only by abstaining and surrendering charged coverage.
3. **H3 — commitment has a bounded winning region.** Stateful commitment can improve recovery only when preparation delay and change hazard make advance allocation useful and the signal is informative enough to repay lost current service.
4. **H4 — commitment is not routing.** Removing transition cost, minimum duration, preparation delay, or future constraint changes the construct into ordinary routing; performance may improve, but that diagnostic arm cannot support C-1570. Support must come from the fully costed, future-constraining arm.
5. **H5 — shared evidence paths share failure.** An output monitor and telemetry path exposed to the same adversarial authority miss coordinated boundary capture more often than an independently rooted witness inside the latter's declared trust boundary.
6. **H6 — no universal independent witness.** Attacks above or inside the witness's trust root remain undetectable by that witness alone; those cells must be marked uncovered and trigger preregistered abstention, quarantine, or escalation rather than contribute to an advantage claim.
7. **H7 — target success can hide system failure.** In some interacting-population worlds, a target-only intervention lowers its registered target while increasing non-target burden or total harm through mediated release.
8. **H8 — ecology is not automatically better control.** A population-aware controller helps only when interaction and observation structure are identifiable and sufficiently stable; a robust target-plus-unknown or minimax null may dominate it.
9. **H9 — no inherited efficiency claim.** Any proposed arm fails unless it remains task/risk noninferior and improves at least one complete resource endpoint after training, monitoring, state, challenges, intervention, fallback, storage, and recovery are charged.

## Systems, scenarios, and tasks

All tracks use artificial generators with public-development, sealed-confirmation, and hostile-transfer partitions. The future implementation must construct fixed system instances first, then nest repeated episodes, rows, challenges, or time steps below those instances. Repeated observations from one fixed instance never increase the independent confirmation count.

### `PAR-X01` system — surface rotation without latent change

Each fixed instance contains $K\geq2$ latent entities. Entity $z$ owns a frozen behavior kernel $p(r\mid z,q)$ over response $r$ to permitted challenge $q$. A visible observation is

$$
o=g_e(z,\eta),
$$

where $g_e$ is a versioned surface operator and $\eta$ is registered noise. Surface operators include vocabulary substitution, serialization changes, feature permutations, nuisance-node relabeling, padded fields, single-operator transforms, and compositions. They may not change the behavior kernel in the identifiable arms.

Partitions are by operator family and composition, not random rows. Development receives a subset $\mathcal E_0$; confirmation receives unseen members of known families $\mathcal E_1$; hostile transfer receives unseen compositions and one new operator family $\mathcal E_2$. A non-identifiable linking control balances the `same` and `different` task labels 1:1 and makes their permitted joint observation and challenge-response distributions identical. A separate unequal-prior diagnostic verifies that performance cannot exceed the prior-only Bayes rule merely by exploiting prevalence.

The task is lineage/function linking with an optional challenge and a required `same`, `different`, or `abstain` output. No generator label, operator ID, latent ID, or hidden behavior parameter may reach an actionable arm.

### `PAR-X02` system — costed lifecycle commitment

Each instance is a partially observed finite-horizon environment with lifecycle state

$$
s_t=(x_t,q_t),
\qquad
q_t\in\{\text{serve},\text{committed},\text{ready}\}.
$$

State $x_t$ contains service demand, a latent change hazard, and a noisy precursor signal. Entering `committed` removes a declared capacity fraction from current service for at least $d_{\min}$ steps and incurs switching/state cost. Only capacity that completed the preparation delay can serve the rare transfer/recovery window. The generator sweeps hazard, precursor sensitivity/specificity, preparation delay, minimum commitment duration, capacity fraction, transfer-window duration, and energy/resource price.

Development interpolates within declared axes. Confirmation uses new Cartesian cells. Hostile transfer combines a hazard and signal-quality regime absent from development. Diagnostic worlds include no change, an instantly recoverable change, a perfectly announced change, and a change that occurs before any policy can prepare.

The task is to maximize accepted service across the whole horizon while satisfying a frozen recovery-success floor. Immediate throughput, false commitment, stranded reserve, and recovery are not collapsed into one hidden reward for evaluation.

### `PAR-X03` system — host/control-plane capture

The focal task model is deterministic and its weights, code digest, and inference configuration remain byte-identical across arms. The generator separately represents true environment state $x_t$, focal observation $o_t$, system action $u_t$, and recorded telemetry $y_t$:

$$
o_t=h(x_t,a^{\mathrm{adv}}_t),
\qquad
u_t=\pi(o_t),
\qquad
y_t=m(o_t,u_t,a^{\mathrm{adv}}_t).
$$

The registered adversarial capability can perturb one or more of: sample selection, retrieval context, evaluator/reward state, router target, tool authority, stopping decision, and telemetry. Attack cells include single-boundary capture; coordinated observation-plus-log capture; router-plus-authority capture; benign shifts matched on marginal output statistics; and attacks above a nominal monitor's trust root.

The task is to detect, contain, and produce an effect-qualified receipt before unauthorized effects exceed a frozen allowance while preserving service under benign shifts. “Model hash unchanged” is a condition of the test, not evidence that the system is safe.

### `PAR-X04` system — intervention-induced competitive release

Each instance contains three artificial threat populations with latent state $n_t=(n_{1,t},n_{2,t},n_{3,t})$. A registered diagnostic generator may use

$$
n_{i,t+1}
=
n_{i,t}\exp\!\left(
r_i-\sum_j a_{ij}n_{j,t}-d_i(u_t)
\right),
$$

where $r_i$ is intrinsic log growth, $A=(a_{ij})$ is the interaction matrix, $u_t$ is the intervention, and $d_i$ is its direct suppression. Alternative generator families must preserve the same causal intervention labels without being algebraic copies of the evaluator model.

Worlds cross no interaction, shared-resource competition, detector-mediated apparent competition, direct interference, facilitation, mutation/substitution, and observation-label substitution without latent release. Development and confirmation use disjoint matrices; hostile transfer withholds at least one interaction sign pattern and one observation operator.

The task is to keep total protected harm and every declared tail constraint below frozen limits. The target-family burden is reported separately and cannot substitute for total harm or unknown mass.

## Arms, baselines, and strongest nulls

Every arm is assigned a stable ID. An applicability matrix must be frozen per track; an inapplicable arm requires a structural reason before confirmation.

### `PAR-X01` arms

1. `S-ERM`: surface-feature empirical-risk minimization without augmentation.
2. `S-AUG`: tuned domain randomization and compositional surface augmentation.
3. `S-GDRO`: group distributionally robust optimization over development operators.
4. `S-CANON`: explicit deterministic canonicalization followed by a matched detector.
5. `S-ADV`: adversarially trained detector under the same transform-query budget.
6. `S-PRIOR`: prior-only Bayes decision rule with no surface or challenge evidence; diagnostic lower-information reference.
7. `S-PROV`: versioned lineage-provenance retrieval without active challenge.
8. `S-CHALLENGE`: active behavior challenge with calibrated abstention.
9. `S-JOINT`: provenance plus challenge plus surface/lineage ledger; the proposed composite arm.
10. `S-ORACLE`: latent identity and operator access; diagnostic ceiling only.

### `PAR-X02` arms

1. `L-FLAT`: compact recurrent flat POMDP policy.
2. `L-OPTION`: learned options with explicit initiation and termination.
3. `L-MOE`: mixture-of-experts gate with matched active capacity.
4. `L-STATIC`: fixed reserve selected in public development.
5. `L-PERIODIC`: periodic preparation/reserve schedule.
6. `L-RESTORE`: checkpoint/restart policy with all restore delay and storage charged.
7. `L-COMMIT`: stateful, minimum-duration commitment policy; proposed arm.
8. `L-ORACLE`: true future-change time; diagnostic ceiling only.

### `PAR-X03` arms

1. `C-OUTPUT`: focal-output anomaly monitor.
2. `C-DUPLICATE`: second monitor sharing observation, deployment, and telemetry roots.
3. `C-PROVENANCE`: signed configuration, retrieval, router, and authority receipts.
4. `C-CANARY`: canary actions and synthetic challenge transactions.
5. `C-RUNTIME`: independently enforced runtime authority policy.
6. `C-WITNESS`: separately rooted observation/effect witness.
7. `C-JOINT`: provenance, canary, runtime policy, and independent witness; proposed composite arm.
8. `C-ORACLE`: complete attacker action and true effect access; diagnostic only.

### `PAR-X04` arms

1. `R-TARGET`: optimizer using only target-family burden.
2. `R-TARGET-UNKNOWN`: target plus explicitly priced unknown mass.
3. `R-SUM`: summed-risk optimizer with frozen weights.
4. `R-MINIMAX`: worst-family/worst-harm robust controller.
5. `R-MPC`: model-predictive controller with estimated population dynamics.
6. `R-GAME`: game-theoretic best-response policy with bounded opponent model.
7. `R-ECO`: population-aware controller estimating direct and mediated effects; proposed arm.
8. `R-ORACLE`: true interaction matrix and latent populations; diagnostic ceiling only.

All ordinary nulls receive serious tuning. A proposed composite cannot claim novelty or advantage by withholding provenance, abstention, hierarchy, robust optimization, unknown-mass monitoring, or intervention models from its competitors.

## Matched budgets and equal information

The future arm registry must enforce all of the following in native units:

1. actionable arms receive byte-identical initial observations, timestamps, masks, task definitions, and available action menus within a track;
2. generator state, future events, operator ID, attack labels, interaction matrix, and confirmation outcomes remain evaluator-only unless the arm is explicitly diagnostic;
3. model parameter count, retained mutable state, pretraining data, training examples, update steps, tuning trials, random restarts, numerical precision, CPU threads, accelerator allocation, and wall-time ceiling are matched or dominated by a preregistered smaller-resource arm;
4. challenge queries in `PAR-X01` are drawn from one common query budget; priors are identical and declared to every actionable arm, unused queries receive no fictional cost, and acquired responses are charged in observations, bytes, latency, and work;
5. all `PAR-X02` arms receive the same total physical/logical capacity. Reserve, checkpoint, expert, or committed slots unavailable to current service count equally; restoration time and stored bytes are charged;
6. all `PAR-X03` arms share the same monitor compute, storage, network, alert, and operator-review ceilings. A separate witness's hardware, communication, key management, and maintenance are not free;
7. all `PAR-X04` arms receive the same intervention opportunities, maximum intervention volume, sensing rate, label budget, state-estimation updates, and action latency;
8. abstention, quarantine, fallback, rollback, escalation, and human review are priced task actions with service and resource consequences;
9. training, evaluation, logging, signatures, provenance, retained state, transmission, artifacts, recovery, and idle reservation remain in the lifecycle denominator; and
10. synthetic operation counts never support an energy claim. Joules require later calibrated execution under one declared hardware and measurement boundary.

Budget violations invalidate the affected paired comparison; they are not repaired by normalizing a favorable result after execution.

## Measurements and units

### `PAR-X01` measurements

1. balanced identity/link error, false-merge rate, false-split rate, abstention rate, and coverage: dimensionless fractions;
2. mean risk and worst-epoch risk
   $$
   R_{\mathrm{worst}}
   =
   \max_{e\in\mathcal E_{\mathrm{test}}}
   \mathbb E[\ell(f(o),z)\mid e],
   $$
   dimensionless under the frozen loss;
3. expected calibration error, full risk--coverage curves, selective risk at frozen coverage points, and excess risk over the prior-only Bayes rule: dimensionless;
4. challenge count, model invocation count, and false-certainty count in non-identifiable worlds: counts;
5. decision and challenge latency: second (s); and
6. retained/transmitted bytes, CPU-s, wall-s, peak B, operation counts, and later J.

### `PAR-X02` measurements

1. accepted service and lost service: normalized service unit (NSU) and NSU-step;
2. recovery/transfer success, false-commitment rate, and stranded-reserve fraction: dimensionless fractions;
3. detection-to-ready latency and committed duration: step and second where calibrated;
4. committed capacity and unavailable current capacity: model-slot and model-slot-step;
5. switch, restore, checkpoint, fallback, and missed-window counts; and
6. training/inference/idle/recovery CPU-s, wall-s, B, operation counts, and later J.

### `PAR-X03` measurements

1. attack detection and containment by registered class, plus false alarms under matched benign shifts: fractions;
2. time to detection and containment: second (s);
3. unauthorized attempted, authorized, executed, contained, and externally verified effects: separate counts;
4. provenance gaps, invalid signatures, independence violations, and attacks above trust root: counts;
5. accepted service loss: NSU and NSU-s;
6. operator review: person-minute; and
7. monitor/witness CPU-s, wall-s, B, messages, network B, artifacts, and later J.

### `PAR-X04` measurements

1. each latent threat population and target/non-target observed burden: normalized burden unit (NBU), never pooled before reporting;
2. unknown/unclassified mass: NBU and fraction of total observed mass;
3. total protected harm and worst-step/tail harm: normalized harm unit (NHU), with weights frozen before confirmation;
4. direct target suppression, mediated non-target change, and observation substitution where identifiable: NBU;
5. rebound time: step or second (s), and area above the frozen harm boundary: NHU-step;
6. intervention volume: normalized intervention unit (NIU), false-positive service effects: NSU; and
7. sensing/control CPU-s, wall-s, B, model updates, intervention count, and later J.

All means are accompanied by independent-instance distributions, worst registered stratum, failures, abstentions, and missing records. No scalar “efficiency score” may hide an unfavorable protected endpoint.

## Ablations and interventions

### `PAR-X01` interventions

1. remove provenance while preserving the classifier;
2. remove active challenge or reduce it to one fixed query;
3. expose only single transforms, then introduce unseen transform compositions;
4. reveal versus withhold the operator epoch from the policy;
5. replace learned representation with deterministic canonicalization;
6. make latent behavior partially distinguishable, exactly non-identifiable, then distinguishable only through an expensive query;
7. corrupt or stale one provenance edge; and
8. charge versus omit signature-bank maintenance as an audit contrast, never as the primary result.

### `PAR-X02` interventions

1. set preparation delay to zero;
2. make commitment instantly reversible;
3. remove the minimum commitment duration;
4. set change hazard to zero, high, or perfectly announced;
5. sweep precursor false-positive and false-negative rates independently;
6. remove current-service opportunity cost;
7. remove idle/retained reserve cost;
8. block checkpoint restoration or make it instantaneous as diagnostic bounds; and
9. shift the transfer window outside development support.

### `PAR-X03` interventions

1. perturb sensing $h$ while leaving telemetry $m$ intact;
2. perturb telemetry $m$ while leaving sensing intact;
3. coordinate $h$ and $m$ under one adversarial root;
4. capture evaluator/reward, router, tool authority, or stopping rule separately;
5. hold focal model weights and code fixed in every attack arm;
6. force duplicate monitors to share or not share dependencies;
7. compromise the independent witness's trust root; and
8. apply benign shifts matched on focal output marginals.

### `PAR-X04` interventions

1. set all off-diagonal interactions to zero;
2. remove only target family 1 while holding direct effects on families 2--3 at zero;
3. reverse or withhold one interaction sign pattern;
4. separate shared-resource, apparent-competition, direct-interference, and facilitation generators;
5. add mutation/substitution with no ecological release;
6. relabel observations without changing latent populations;
7. hide versus reveal unknown mass to the controller;
8. freeze versus adapt the interaction estimator; and
9. vary intervention intensity without changing the opportunity count.

Ablation episodes remain nested within their fixed system instance. They are paired contrasts, not additional independent confirmation units.

## Statistical analysis plan

1. Freeze generator families, instance grammar, public/confirmation/hostile partitions, arm code, tuning budget, primary contrasts, task/risk margins, resource margins, multiplicity family, missing-data rules, and analysis code before revealing any confirmation seed.
2. Generate independent fixed system instances first. Pair all applicable arms and ablations within each instance. Aggregate repeated episodes, rows, queries, and time steps to one registered instance-level value per endpoint before confirmatory inference.
3. Determine confirmation instance counts from public-pilot variance, a frozen smallest relevant effect, desired power, and expected invalid-run rate. This prose does not invent those counts and supplies no pilot observation.
4. The primary gate is hierarchical: protected task/risk noninferiority first; then superiority on at least one complete resource endpoint. A resource comparison is not tested when the protected gate fails.
5. For continuous paired endpoints, report paired instance-level differences with two-sided 95% bootstrap confidence intervals using the independent instance as the resampling unit. For bounded failure/success endpoints, report paired risk differences and compatible simultaneous intervals. Heavy-tail endpoints additionally report registered quantiles and worst stratum.
6. Control family-wise error at 0.05 with Holm correction across the four track-level proposed-versus-strongest-null primary comparisons. Within-track diagnostic contrasts are labeled exploratory unless included in a separately frozen hierarchy.
7. `PAR-X01` reports mean, worst-epoch, unseen-composition, equal-prior non-identifiable, and unequal-prior diagnostic strata. Any accepted-link result above chance in the equal-prior non-identifiable arm, or any result beating prior-only Bayes risk in either non-identifiable arm, is a protocol failure. Selective improvement must be reported as a preregistered risk--coverage trade rather than accuracy on an unreported accepted subset.
8. `PAR-X02` estimates the service--recovery--resource Pareto frontier across hazard/signal/preparation cells and tests the preregistered intermediate winning region; averages across diagnostic no-change and perfect-signal worlds cannot establish it.
9. `PAR-X03` stratifies by attacked boundary, coordinated versus single compromise, witness-root status, and benign shift. Detection credit requires an effect-qualified receipt before the frozen harm allowance is exceeded.
10. `PAR-X04` decomposes direct target change, mediated non-target change, and observation substitution only in generator families where the intervention design identifies that decomposition. Otherwise it reports total effects and labels mediation unidentified.
11. Calibration curves, risk--coverage curves, invalid runs, timeouts, abstentions, missing telemetry, and all protected hostile-transfer strata are reported. No post-hoc seed, world, attack, interaction matrix, or failed arm may be removed.
12. Raw append-only events, configuration, environment receipt, source and artifact hashes, and analysis outputs must permit deterministic recomputation. Any discrepancy invalidates the run pending a documented new version; it cannot be silently repaired.

## Promotion, rejection, and kill rules

No track can promote a biological statement. At most, a future admissible execution can support its bounded central AI claim inside the frozen artificial population.

A track supports that bounded claim only if all of the following hold:

1. the proposed arm passes every preregistered task, protected-risk, calibration, and safety noninferiority margin;
2. it beats every applicable mature null on at least one complete resource endpoint with the simultaneous confidence interval beyond the frozen relevance margin;
3. the advantage survives sealed confirmation and every mandatory hostile-transfer stratum, or a preregistered support gate abstains before a protected violation and all fallback cost remains charged;
4. the result is carried by independent fixed system instances, not rows, time steps, queries, threats, or repeated episodes;
5. no hidden generator state, future event, attack label, operator ID, interaction matrix, or evaluator output reaches an actionable arm;
6. tuning, challenge, reserve, monitor, witness, intervention, provenance, storage, operator, fallback, recovery, idle, and artifact burdens are complete; and
7. artifacts deterministically replay under the declared software/hardware boundary.

Track-specific rejection rules are:

- **C-1569 / `PAR-X01`:** reject if augmentation, canonicalization, group DRO, or provenance alone matches `S-JOINT`; if gains vanish on unseen compositions; if the arm beats prior-only Bayes in a non-identifiable world, exceeds chance among accepted equal-prior link decisions, or hides the abstention cost by omitting coverage; or if false merges merely become false splits.
- **C-1570 / `PAR-X02`:** reject if an option policy, fixed reserve, or checkpoint arm dominates the service--recovery--resource frontier; if the gain exists only after commitment cost is removed; or if the claimed region reverses under the sealed hazard/signal sweep.
- **C-1571 / `PAR-X03`:** reject if nominally independent evidence shares the attacked dependency; if benign-shift false alarms exhaust service/operator budgets; if the receipt does not bind completed effects; or if a compromised-root cell is counted as covered instead of triggering the preregistered abstention, quarantine, or escalation path before protected harm, with all fallback cost charged.
- **C-1572 / `PAR-X04`:** reject if robust target-plus-unknown or minimax control matches total harm more cheaply; if apparent release is only observation-label substitution; if the population controller needs the true interaction matrix; or if target improvement is accompanied by a protected total/tail-harm violation.

The entire fixture is killed or retired if any of these occurs:

1. a mature conventional stack implements the same effective behavior at lower complete cost;
2. results require post-confirmation threshold, margin, baseline, stratum, or generator changes;
3. resource savings come from lower information, action authority, service, capacity, tuning, or intervention access;
4. an aggregate score hides a failed lineage, lifecycle, trust-boundary, threat, or unknown-mass stratum;
5. a synthetic operation count is presented as energy or a source organism's efficiency is transferred without measurement;
6. source reproduction, fixture construction, public smoke, and confirmation authority are conflated; or
7. the raw records, receipts, code, environment, and analysis cannot reproduce the reported comparison.

Until those gates are executed and passed, `F-028`, `PAR-X01`--`PAR-X04`, and [C-1569](../../research/claims.md#c-1569)--[C-1572](../../research/claims.md#c-1572) remain **NO_RESULT**, specification-only, and not workstation-executable.

## Future artifact contract

A later implementation must add, without weakening this prose contract:

1. canonical schemas for instance definitions, arm registry, budgets, raw events, failures, summaries, and receipts;
2. content-addressed public and sealed seed packs with domain-separated random streams;
3. a manifest that declares `smoke-ready` before any bounded plumbing run and `workstation-ready` only after full confirmation requirements exist;
4. deterministic prepare, validate, smoke, run, resume, corrupt-resume, and analyze commands;
5. tests proving partition separation, hidden-state denial, independent-instance accounting, budget enforcement, fail-closed unknown enums, and exact claim scope;
6. append-only raw artifacts and an analysis that refuses missing arms, incomplete strata, duplicate instances, unregistered exclusions, and hash mismatch; and
7. a calibrated workstation-energy method if and only if an energy comparison is later attempted.

None of those artifacts exists at this specification checkpoint.
