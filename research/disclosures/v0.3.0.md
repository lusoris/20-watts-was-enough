# Research-output disclosure — v0.3.0

- **Output:** repository, full-book and experiment-container snapshot `v0.3.0`
- **Record date:** 2026-08-30
- **Authority:** maintained concept, evidence framework, protocols, publication
  tooling and bounded development harnesses; no new claim promotion,
  independent review, claim-eligible workstation result, or measured
  performance or energy result

## Contributors and responsibility

| Actor | Recorded role |
| --- | --- |
| [`@lusoris`](https://github.com/lusoris) | Project and concept originator; research direction; requirements and editorial decisions; repository owner; accountable release approver |
| OpenAI Codex desktop agent, including its multi-agent execution | Material automated tool used for repository inspection, official-source discovery, drafting, code changes, tests, browser-based site review and authenticated GitHub operations; not an author or accountable approver |

Creating the immutable tag authorizes that source as a release candidate.
Accountable publication approval is evidenced only by the successful
maintainer-controlled release workflow and resulting GitHub Release; a tag
without a release is not an approved output. No independent scientific
reviewer or second human line-by-line review is recorded for this snapshot.

## Funding and material support

No grant, employer or institutional research funding has been declared in the
repository for this snapshot. Material infrastructure used includes the
maintainer's workstation, GitHub repository, Actions, Pages and Container
Registry, Cloudflare DNS and proxying for the public domain, Docker, and OpenAI
Codex. This record does not infer the commercial terms, personal subscription
costs or in-kind value of those services.

## Competing interests

The maintainer owns and governs the repository and has public donation links;
those interests are visible in the project surfaces. No separate financial or
non-financial competing-interest declaration was supplied for `v0.3.0`.
Accordingly the status is **not separately declared**, not “none known”.

## Material AI, automation and external services

| Tool, service or source set | Material use | Verification boundary |
| --- | --- | --- |
| OpenAI Codex desktop agent, used on 2026-08-29 and 2026-08-30 | Drafted and revised prose and code, delegated bounded reviews, searched official sources, ran local validation, inspected the live Pages reader and operated authenticated GitHub interfaces under maintainer direction | Stable claims and sources remain subject to repository ledgers and validators. Codex performed the recorded checks; no independent human line review is claimed. |
| GitHub and GitHub Actions | Canonical Git host, issue, release and governance surfaces, CI, CodeQL, Scorecard, Pages publication, release attestations and OCI publication | Remote settings, workflow states, release assets, attestations and image identities are verified through GitHub rather than inferred from files. Immutable releases were enabled for future releases on 2026-08-30. Main ruleset `21746706` was read back active with no bypass, required pull requests, strict CI, linear history and CodeQL. The completed `v0.3.0` release must still be checked for immutability. |
| Cloudflare | DNS, proxy, HTTPS and path-preserving apex-to-`www` redirect for `cordana.dev` | Root and book routes were checked through the live HTTP and HTTPS boundary on 2026-08-30. Cloudflare controls the public-domain edge; GitHub Pages remains the generated-site host. |
| Docker | Built and executed the locked PDF renderer, static Go tooling image and the closed Fixture 007 and Fixture 019 runtime images | Renderer comparison and runtime smoke, analysis and validation execution demonstrate publication-path conformance only. Every released experiment image carries the `NO_RESULT` authority boundary. |
| Buildx 0.36.1, BuildKit 0.32.2, Chrome for Testing 152.0.7977.64, Puppeteer 25.9.0 and the renderer's pinned Linux `amd64` container inputs | Fixed the builder, browser executable, browser-library environment, Node runtime, system libraries and fonts used to generate the release PDF | The maintained renderer lock binds the Buildx revision, BuildKit image, browser archive and executable bytes, and runtime OCI inputs. The Go publication command requires two byte-identical isolated fresh renders before installing the pair. Both containers share one read-only dependency tree realized by clean release CI from `package-lock.json`; the check is conditional on that realization and is not a claim about every host. |
| Go 1.27.0, Node.js 26.8.1, npm 12.0.2, W3C, OCI and project documentation | Supplied version-specific implementation rules for tooling, runtime, interface and release work; the npm registry archive was independently checked and locked by URL, size and SHA-256 because the Node distribution includes npm 11.19.0 | Official and current documentation informed implementation. Archive identity and local checks do not establish remote publication or a scientific result. |
| IBEX, The Turing Way, JOSS, ReScience C, Real World Data Science, research-compendium, FAIR4RS, RO-Crate, Weblate, po4a, Quarto and Distill sources | Supplied the external comparison set for release, publication, translation, interface and typed-specialist work | Comparison sources identified gaps; repository policy and decisions remain controlling. |
| Primary experiments, systematic map and scientific responses cited in the [common-mycorrhizal-network section](../audits/2026-08-05-fungal-networks-resource-allocation.md#common-mycorrhizal-networks-topology-transfer-and-disputed-inference); user-supplied Wikipedia article | Bounded the common-mycorrhizal-network update; the Wikipedia article served only as a search lead | Repository claims retain their primary-source and evidence-ledger requirements. Search output and secondary summaries are not evidence. |
| [`lusoris/k8s` commit `91ef1abda09df5361859bd1010703113b3d439da`](https://github.com/lusoris/k8s/tree/91ef1abda09df5361859bd1010703113b3d439da) | Supplied the read-only engineering-transfer audit of QDO, llama-swap, LiteLLM admission and Paperclip dispatch; focused local tests were rerun | The audit records reusable policy shapes and stress situations only. No cluster code, live-state claim, hardware constant, scientific result or performance comparison was imported. |

## Pre-release evidence and publication conditions

The redesigned Pages portal and focused reader were inspected locally at
`1440`, `390` and `320` CSS-pixel widths. The inspection covered line measure,
title-preserving navigation, wrapping and horizontal overflow. A separate
headless-browser regression now requires every Mermaid wrapper to retain one
non-empty SVG after React's development StrictMode effect cycle. These are
interface and rendering checks, not reader-comprehension or accessibility-
conformance results.

The last already-published `origin/main` aggregate evidence available while
this record was drafted is bound to commit
`177173286b216ce05b16865109614a156dad29e5`. It does not verify the later
`v0.3.0` candidate edits. Source-level tests exercise stable file reads,
translation manifests, release-image admission, tag attachment, current-build
attestation and rejection of missing existing-image provenance,
repository-label reconciliation and runtime numerical identity, but a
passing simulated case is not evidence that the corresponding remote release
operation occurred.

Publication requires the aggregate gate, an observed-complete deterministic
tag-bound PDF, checksum and attestation verification, and offline execution of
every admitted OCI digest to pass on the exact tag. The release workflow must
record the tag, commit, run identity, PDF digest and observed diagram count,
asset checksums and final OCI digests. This source disclosure does not claim
that those post-tag checks have completed. It also does not claim that the
three first-use GHCR packages are public until unauthenticated exact-digest
pulls pass with an empty Docker configuration. Those checks establish a
reproducible publication boundary; they do not create a scientific result or
an independent review.
