# Extend the maintained gitleaks defaults without broad path exclusions. GitHub
# secret scanning and push protection remain the first line of defense; this
# file is ready for local or CI full-history scans using an integrity-pinned
# implementation. Add only exact-value allowlists after verifying a false
# positive is a public fixture rather than a credential.
[extend]
useDefault = true
